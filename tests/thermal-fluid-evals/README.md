# Thermal-Fluid Eval Fixtures

These fixtures are lightweight prompt scenarios for checking whether the skill preserves thermal-fluid rigor. They do not execute model evaluation by themselves; they define the behaviors a reviewer should expect from the skill.

The repository validation script checks that the fixture file contains exactly ten scenarios and that each scenario has multiple expected checks.
